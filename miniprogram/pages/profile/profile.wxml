<!--
  个人中心页 — 暖暖梦幻风格
  角色展示区 + 库存统计 + 设置列表
-->
<view class="page-profile">

  <!-- 暖暖角色展示头部 -->
  <view class="profile-header">
    <!-- 装饰背景 -->
    <view class="profile-deco profile-deco-1"></view>
    <view class="profile-deco profile-deco-2"></view>
    <view class="profile-deco profile-deco-3"></view>

    <view class="profile-header-inner">
      <!-- 暖暖角色形象展示 -->
      <view class="character-showcase">
        <image
          class="character-showcase-img"
          src="/images/nuannuan-character.jpeg"
          mode="aspectFill"
        />
      </view>
      <view class="profile-info">
        <text class="profile-title">暖暖美妆库</text>
        <text class="profile-subtitle">用心守护每一件美妆 ✧</text>
      </view>
    </view>
  </view>

  <!-- 库存统计 -->
  <view class="section" wx:if="{{categoryStats.length > 0}}">
    <text class="section-label">📦 库存统计</text>
    <view class="category-stats">
      <view
        wx:for="{{categoryStats}}"
        wx:key="name"
        class="cat-stat-card cat-bg-{{index % 3}}"
      >
        <text class="cat-stat-num">{{item.count}}</text>
        <text class="cat-stat-name">{{item.name}}</text>
      </view>
    </view>
  </view>

  <!-- 设置列表 · 提醒设置 -->
  <view class="section">
    <text class="section-label">⏰ 提醒设置</text>
    <view class="settings-group card">
      <view class="setting-item" bindtap="onAdvanceDaysTap">
        <text class="setting-name">提前提醒天数</text>
        <view class="setting-right">
          <text class="setting-value">{{advanceDays}} 天</text>
          <text class="setting-arrow">›</text>
        </view>
      </view>
      <view class="setting-item">
        <text class="setting-name">推送通知</text>
        <switch
          class="setting-switch"
          checked="{{enablePush}}"
          bindchange="onPushToggle"
          color="#5BA8D0"
        />
      </view>
      <view class="setting-item" wx:if="{{enablePush}}" bindtap="onPushFrequencyTap">
        <text class="setting-name">推送频率</text>
        <view class="setting-right">
          <text class="setting-value">{{pushFrequency === 'daily' ? '每天' : '每周'}}</text>
          <text class="setting-arrow">›</text>
        </view>
      </view>
    </view>
  </view>

  <!-- 设置列表 · 管理 -->
  <view class="section">
    <text class="section-label">⚙ 管理</text>
    <view class="settings-group card">
      <view class="setting-item" bindtap="goCategory">
        <text class="setting-name">分类管理</text>
        <view class="setting-right">
          <text class="setting-arrow">›</text>
        </view>
      </view>
    </view>
  </view>

</view>
